apiVersion: v1
kind: Service
metadata:
  name: backend-app-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80           # Service port used by Envoy in the cluster definition
      targetPort: 5000   # Backend container port
  type: ClusterIP       # Internal service; no external exposure needed
